# Push current version to live

## Current state
- Working directory: `/var/www/hw/dev` (branch `dev`)
- Uncommitted changes: app/api/novelai/route.ts, app/dreambound-app.tsx, app/features/characters/character-area.tsx, app/globals.css, features/chat/chat-workspace.tsx, features/context/context-workspace.tsx, lib/generation/compile-context.ts, lib/relationships/schema.ts, lib/relationships/storage.ts, lib/worlds/builtins.ts, lib/worlds/builtins/senako.ts, lib/worlds/builtins/valerie.ts, tests/context-compiler.test.mjs, public/assets/Heather/valerie-whiteclaw.png
- Last commit: `645be7f 0.8.0 milestone: Echoes Remembered`

## Plan
1. In `/var/www/hw/dev`:
   - `git pull` to ensure `dev` is up to date.
   - Stage only the intended files (exclude `.kilo/` unless explicitly wanted).
   - Commit with a concise message.
   - `git push origin dev`.
2. In `/var/www/hw/rp` (main checkout):
   - `git fetch origin`
   - `git merge --ff-only origin/dev`
   - `git push origin main`
3. Run `/usr/local/sbin/howlingwhispers-promote` to build in a fresh worktree, run tests/lint, perform health check, atomically switch symlink, and restart live service.
4. After deploy, restart sandbox: `systemctl restart thehowlingwhispers-dev.service`.
5. Verify live release returns 200 and correct bundle hash. Hard-refresh to clear stale cache.

## Constraints
- Do not skip tests, lint, or build.
- Do not force-push or skip the promote script.
- Do not commit secrets/tokens.
- If merge is not fast-forward, stop and report.
